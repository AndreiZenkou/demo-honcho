const SOURCE_FILE_PATH = `${process.cwd()}/data/source/test-xml.xml`;
const DESTINATION_FILE_PATH = `${process.cwd()}/data/source/test-json.json`;
const LOG_FILE_PATH = `${process.cwd()}/data/log/logs.txt`;

module.exports = {
	LOG_FILE_PATH,
	SOURCE_FILE_PATH,
	DESTINATION_FILE_PATH
}
